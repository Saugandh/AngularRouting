import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';
import { DashboardClass } from './Dash/dash.component';
@Component({
  selector: 'my-app',
  template: `      <dash-tag>...inside dash tag</dash-tag>
 `
})
export class AppComponent  {  }
